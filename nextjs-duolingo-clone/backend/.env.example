DATABASE_URL=
CLERK_SECRET_KEY=
CLERK_PUBLISHABLE_KEY=
STRIPE_API_KEY=
STRIPE_WEBHOOK_SECRET=
# Comma-separated list of allowed browser origins (native apps don't send Origin, so they aren't affected by this)
ALLOWED_ORIGINS=http://localhost:3000
# Public URL of the web app, used to build Stripe Checkout/Billing Portal return URLs
WEB_APP_URL=http://localhost:3000
PORT=4000
